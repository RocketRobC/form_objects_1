= flash[:notice]

%h1 Coffee Types

%table
  %thead
    %tr
      %th Name
      %th Recipe
      %th
      %th
      %th

  %tbody
    - @coffee_types.each do |coffee_type|
      %tr
        %td= coffee_type.name
        %td= coffee_type.recipe
        %td= link_to 'Show', coffee_type_path(coffee_type)
        %td= link_to 'Edit', edit_coffee_type_path(coffee_type)
        %td= link_to 'Destroy', coffee_type, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Add a new type of coffee', new_coffee_type_path
